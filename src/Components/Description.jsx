function Description() {
  const tourDetailsStyles = {
    display: "flex",
    flexDirection: "columns",
    justifyContent: "space-between",
  };
  return (
    <div>
      <div className={tourDetailsStyles}></div>
    </div>
  );
}
